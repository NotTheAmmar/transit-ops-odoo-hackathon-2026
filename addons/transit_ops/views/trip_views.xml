<?xml version="1.0" encoding="utf-8"?>
<!--
    Trip Views
    ==========
    Owner: Person B

    TODO:
    1. Form view:
       - Domain on vehicle_id: [('status','=','available')] to only show Available vehicles
       - Domain on driver_id: [('status','=','available'),('is_license_expired','=',False)]
       - Completion fields (actual_distance, fuel_consumed, final_odometer) only editable
         when state == 'dispatched' — use invisible="state != 'dispatched'"
       - Action buttons: Dispatch (state=draft), Complete (state=dispatched),
         Cancel (state in draft/dispatched)

    2. Kanban view: Group by state for visual dispatch board

    3. List view: Color-code by state
-->
<odoo>
    <data>

        <!-- ── List View ─────────────────────────────────────────────────────── -->
        <record id="view_transit_trip_list" model="ir.ui.view">
            <field name="name">transit.trip.list</field>
            <field name="model">transit.trip</field>
            <field name="arch" type="xml">
                <list string="Trips"
                      decoration-success="state == 'completed'"
                      decoration-info="state == 'dispatched'"
                      decoration-muted="state == 'cancelled'">
                    <field name="name"/>
                    <field name="source"/>
                    <field name="destination"/>
                    <field name="vehicle_id"/>
                    <field name="driver_id"/>
                    <field name="cargo_weight"/>
                    <field name="planned_distance"/>
                    <field name="state" widget="badge"
                           decoration-secondary="state == 'draft'"
                           decoration-info="state == 'dispatched'"
                           decoration-success="state == 'completed'"
                           decoration-danger="state == 'cancelled'"/>
                </list>
            </field>
        </record>

        <!-- ── Form View ─────────────────────────────────────────────────────── -->
        <record id="view_transit_trip_form" model="ir.ui.view">
            <field name="name">transit.trip.form</field>
            <field name="model">transit.trip</field>
            <field name="arch" type="xml">
                <form string="Trip">
                    <header>
                        <!-- TODO (Person B): Add domain-restricted buttons -->
                        <button name="action_dispatch" string="Dispatch" type="object"
                                class="btn-primary"
                                invisible="state != 'draft'"/>
                        <button name="action_complete" string="Complete Trip" type="object"
                                class="btn-success"
                                invisible="state != 'dispatched'"/>
                        <button name="action_cancel" string="Cancel" type="object"
                                class="btn-danger"
                                invisible="state not in ('draft', 'dispatched')"/>
                        <field name="state" widget="statusbar"
                               statusbar_visible="draft,dispatched,completed"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1><field name="name" readonly="1"/></h1>
                        </div>
                        <group>
                            <group string="Route">
                                <field name="source"/>
                                <field name="destination"/>
                                <field name="planned_distance"/>
                            </group>
                            <group string="Assignment">
                                <!-- TODO (Person B): Add domains to these fields -->
                                <field name="vehicle_id"
                                       domain="[('status','=','available')]"
                                       context="{'show_address': 0}"/>
                                <field name="driver_id"
                                       domain="[('status','=','available'),('is_license_expired','=',False)]"/>
                                <field name="cargo_weight"/>
                            </group>
                        </group>
                        <!-- Completion section — only relevant when dispatched -->
                        <group string="Completion Details" invisible="state not in ('dispatched','completed')">
                            <field name="actual_distance" readonly="state == 'completed'"/>
                            <field name="fuel_consumed" readonly="state == 'completed'"/>
                            <field name="final_odometer" readonly="state == 'completed'"/>
                            <field name="fuel_efficiency" readonly="1"/>
                        </group>
                        <notebook>
                            <page string="Fuel Logs">
                                <field name="fuel_log_ids">
                                    <list>
                                        <field name="date"/>
                                        <field name="liters"/>
                                        <field name="cost"/>
                                    </list>
                                </field>
                            </page>
                            <page string="Expenses">
                                <field name="expense_ids">
                                    <list>
                                        <field name="date"/>
                                        <field name="expense_type"/>
                                        <field name="amount"/>
                                        <field name="description"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <chatter/>
                </form>
            </field>
        </record>

        <!-- ── Kanban View ───────────────────────────────────────────────────── -->
        <record id="view_transit_trip_kanban" model="ir.ui.view">
            <field name="name">transit.trip.kanban</field>
            <field name="model">transit.trip</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" class="o_kanban_small_column">
                    <field name="name"/>
                    <field name="source"/>
                    <field name="destination"/>
                    <field name="vehicle_id"/>
                    <field name="driver_id"/>
                    <field name="state"/>
                    <field name="cargo_weight"/>
                    <templates>
                        <t t-name="card">
                            <div class="oe_kanban_card">
                                <strong><field name="name"/></strong>
                                <div>
                                    📍 <field name="source"/> → <field name="destination"/>
                                </div>
                                <div>
                                    🚛 <field name="vehicle_id"/>
                                </div>
                                <div>
                                    👤 <field name="driver_id"/>
                                </div>
                                <div>
                                    📦 <field name="cargo_weight"/> kg
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <!-- ── Search View ───────────────────────────────────────────────────── -->
        <record id="view_transit_trip_search" model="ir.ui.view">
            <field name="name">transit.trip.search</field>
            <field name="model">transit.trip</field>
            <field name="arch" type="xml">
                <search string="Search Trips">
                    <field name="name"/>
                    <field name="source"/>
                    <field name="destination"/>
                    <field name="vehicle_id"/>
                    <field name="driver_id"/>
                    <separator/>
                    <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                    <filter string="Dispatched" name="dispatched" domain="[('state','=','dispatched')]"/>
                    <filter string="Completed" name="completed" domain="[('state','=','completed')]"/>
                    <filter string="Cancelled" name="cancelled" domain="[('state','=','cancelled')]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter string="State" name="group_state" context="{'group_by':'state'}"/>
                        <filter string="Vehicle" name="group_vehicle" context="{'group_by':'vehicle_id'}"/>
                        <filter string="Driver" name="group_driver" context="{'group_by':'driver_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- ── Window Action ─────────────────────────────────────────────────── -->
        <record id="action_transit_trip" model="ir.actions.act_window">
            <field name="name">Trips</field>
            <field name="res_model">transit.trip</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="search_view_id" ref="view_transit_trip_search"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first trip!
                </p>
            </field>
        </record>

    </data>
</odoo>
